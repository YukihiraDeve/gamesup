spring:
  config:
    activate:
      on-profile: test
  datasource:
    generate-unique-name: true
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: false

gamesup:
  jwt:
    secret: test-only-signing-key-with-at-least-32-bytes
  recommendation:
    service-key: test-only-fastapi-key
